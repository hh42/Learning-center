### This folder contain preconfigured :

* apache2, php5.6, phpmyadmin, mysql : multicontainer web environment
* require bootlocal.sh setup into vm and docker-compose pre-installed
* copy this folder into shared /www
